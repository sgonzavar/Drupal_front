/**
 * @file
 * design behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.design = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
